大表对DDL(create)操作(DML)

建立索引需要长时间
风险
<5.5会锁表
>=5.5不会锁表但会引起主从延迟

修改表结构需要长时间锁表
风险
会造成长时间的主从延迟(主从是单线程,主库需要多少时间,从库就需要多少时间)
影响正常数据操作(DML语句可能会 )

如何处理数据库重的大表

分表:
难点:
分区表主键选择
分表后跨分区数据的查询和统计

大表需要历史数据归档(分库分表应该作为最后的手段)